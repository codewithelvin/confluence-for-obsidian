{
  "certified": false,
  "notes": "The marker is unwrapped so the text stays readable, which means it cannot be reproduced and the page becomes read-only. Preserving it as a placeholder pair would keep such pages editable — the main lever on risk R1 if real pages turn out to use inline comments heavily."
}
